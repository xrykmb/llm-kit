# 示例知识库：模块化 RAG

检索增强生成（RAG）把「找资料」和「写答案」分开。

步骤：

1. 加载文档（txt / markdown）。
2. 切成带重叠的文本块。
3. 用检索器找出和问题最相关的块。本仓库默认用 TF-IDF，不依赖向量数据库。
4. 把检索到的块放进提示词，交给 DeepSeek 生成。
5. 可选 Chain-of-Thought：先逐步推理，再给出「最终答案」。

资料不足时模型应回答不知道，不要编造。
